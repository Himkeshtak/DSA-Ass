# include <stdio.h>
# include <string.h>

int main()
{   
    char stack[20];
    //char in_arr[20]
    scanf("%s", stack);
    for (int i=0;i<strlen(stack[]))
    {
        if(stack[i] != 'a' || stack[i] != 'b' || stack[i] != 'c' || stack[i] != 'd' || stack[i] != 'e' || stack[i] != 'f')
        {

        }

    }

    if()
    {
        printf("True");
    }
    else
    {
        printf("False");
    }
    return 0;
}